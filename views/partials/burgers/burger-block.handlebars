<li>
    {{!-- Burger Name --}}
    {{burger_name}}

    {{!-- Devour Button --}}
    {{#if devoured}}
    <button class="btn btn-outline-dark btn-sm change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
        DEVOUR!
    </button>
    {{/if}}
</li>